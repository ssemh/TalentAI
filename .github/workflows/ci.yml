name: ci

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore YGA.sln

      - name: Build
        run: dotnet build YGA.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test YGA.sln --configuration Release --no-build
