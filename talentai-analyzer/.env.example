## Frontend API configuration (Vite)
## Vars are read at build/dev-server start time.
##
## Base path for backend (use Vite proxy in dev)
# VITE_API_BASE=/api/v1
##
## Analyze endpoint path under API_BASE
# VITE_ANALYZE_PATH=/analyze

